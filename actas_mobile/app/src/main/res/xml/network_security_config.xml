<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite HTTP sin cifrar solo hacia el backend local de desarrollo
  (10.0.2.2 = host de la máquina visto desde el emulador de Android).
  Todo el resto del tráfico (Supabase, Storage) sigue exigiendo HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
